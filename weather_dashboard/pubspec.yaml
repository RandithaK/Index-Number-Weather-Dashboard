name: weather_dashboard
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Broadened SDK compatibility

dependencies:
  flutter:
    sdk: flutter
  
  # Using DIO http client
  dio: ^5.4.3+1 # Or latest version

  # Keep shared_preferences for caching
  shared_preferences: ^2.2.3 
  
  # Keep intl for date formatting
  intl: ^0.20.2 

  # 'http' package has been removed
  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # (No changes needed to the rest of the file)